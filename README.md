# GitHub Code Interviewer

> AI-powered interview prep from YOUR own GitHub code.

## What it does
Paste a GitHub repository URL, tag the important files, choose focus areas, and get personalized technical interview questions generated by Google Gemini — all about decisions YOU made in YOUR codebase.

## Features
- 🔐 **GitHub OAuth** — sign in with your GitHub account
- 🗂️ **File tagging** — mark files as Core Logic / Boilerplate / Config / Tests
- 🎯 **Focus areas** — Architecture, Error Handling, Performance, Security
- 🤖 **AI Questions** — Gemini generates contextual questions from your actual code
- 💬 **Chat-style UI** — answer one question at a time
- ✅ **AI Evaluation** — get a score + detailed feedback per answer
- 📝 **Annotations** — add your own notes after seeing AI feedback
- 📊 **Sessions** — all interviews saved, review anytime

## Tech Stack
- **Next.js 14** (App Router, TypeScript)
- **NextAuth.js v4** (GitHub OAuth)
- **MongoDB + Mongoose**
- **Google Gemini AI**
- **Tailwind CSS**

## Setup

### 1. Clone and install
```bash
npm install
```

### 2. Environment variables
Copy `.env.local.example` to `.env.local` and fill in:

```env
MONGODB_URL=your_mongodb_connection_string
NEXTAUTH_SECRET=any_random_secret_string
NEXTAUTH_URL=http://localhost:3000
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret
GEMINI_API_KEY=your_google_ai_studio_api_key
```

### 3. GitHub OAuth App
1. Go to [github.com/settings/developers](https://github.com/settings/developers) → **OAuth Apps** → **New OAuth App**
2. Set **Authorization callback URL** to: `http://localhost:3000/api/auth/callback/github`
3. Copy the Client ID + Client Secret into `.env.local`

### 4. Run dev server
```bash
npm run dev
```
Visit `http://localhost:3000`

## Project Structure
```
src/
├── app/              # Next.js App Router pages + API routes
├── components/       # Reusable React components
├── lib/              # DB connection, auth config, GitHub helper, Gemini helper
├── models/           # Mongoose schemas
└── types/            # Shared TypeScript types
```

## How to use
1. Sign in with GitHub
2. Go to **New Interview** → paste a public GitHub repo URL
3. Tag important files (Core Logic, Config, etc.)
4. Pick focus areas
5. Click **Generate Questions**
6. Answer each question → get AI feedback + score
7. Add personal notes/corrections
8. Review all sessions on the Dashboard
